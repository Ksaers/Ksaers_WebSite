version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - ksaers-network

networks:
  ksaers-network:
    driver: bridge
